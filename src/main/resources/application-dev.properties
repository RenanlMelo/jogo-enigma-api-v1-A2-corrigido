# DEV usa PostgreSQL real para manter paridade com homologacao.
spring.datasource.url=${DB_URL:jdbc:postgresql://localhost:5432/enigma_dev}
spring.datasource.username=${DB_USER:enigma}
spring.datasource.password=${DB_PASSWORD:enigma_dev}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
